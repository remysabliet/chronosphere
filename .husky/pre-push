#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Validate branch name
./scripts/validate-branch-name.sh

# Run all unit tests with coverage thresholds (fails if any service drops below 80%)
pnpm run test:node:coverage
pnpm run test:python:unit
