import { describe, it } from 'vitest';

describe('Content Management Service — Integration', () => {
  it.skip('connects to database — requires live DB; write manually as integration test', () => {});
});
